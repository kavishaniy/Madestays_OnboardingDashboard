# AI sessions

This dashboard was built in **GitHub Copilot Chat (agent mode) in VS Code**, using the Claude Sonnet 5 model. VS Code's Copilot Chat has no `/export` command or share-link feature, so instead of a raw transcript this folder contains `copilot-session-summary.md`, a factual summary of the working session written from the actual chat history before submitting.

